/*#include<stdio.h>
#include<conio.h>

void main()
{
    int sum=0,a[5],i;
    printf("ENTER THE ELEMENTS OF AN ARRAY of size 5:\n");

    for(i=0;i<5;i++)
    {
        printf("a[%d]=",i);
        scanf("%d",&a[i]);
        if(a[i]%10==0&&a[i]%15!=0)
        sum+=a[i];
        printf("\n");
    }

    printf("THE SUM OF ELEMENTS DIVISIBLE BY 10 BUT NOT BY 15 IS %d",sum);
    getch();
}
*/
/*
#include<stdio.h>
#include<conio.h>

void main()
{
    int sum=0,a[3][3],i,j;
    printf("ENTER THE ELEMENTS OF 3x3 MATRIX:\n");

    for(i=0;i<3;i++)
    {
        for(j=0;j<3;j++)
        {printf("a[%d][%d]=",i,j);
        scanf("%d",&a[i][j]);
        if(a[i][j]%7==0&&a[i][j]%5!=0)
        sum+=a[i][j];
        }
    }

    printf("THE SUM OF ELEMENTS DIVISIBLE BY 7 BUT NOT BY 5 IS %d",sum);
    getch();
}

*/
