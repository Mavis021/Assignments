/*#include<stdio.h>
#include<conio.h>

int findLowest(int, int, int);
int findHighest(int, int, int);

void main()
{
    int a,b,c;
    printf("ENTER ANY THREE NUMBERS:\n");
    scanf("%d%d%d",&a,&b,&c);

    printf("THE HIGHEST NUMBER AMONG %d , %d and %d is %d\n",a,b,c,findHighest(a,b,c));
    printf("THE LOWEST NUMBER AMONG %d , %d and %d is %d",a,b,c,findLowest(a,b,c));

    getch();
}

int findLowest(int a, int b, int c)
{
    int p,q;
    p= ((a<b)? a:b);
    q= ((p<c)? p:c);
    return(q);
}

int findHighest(int a, int b, int c)
{
    int p,q;
    p= ((a>b)? a:b);
    q= ((p>c)? p:c);
    return(q);
}

*/
