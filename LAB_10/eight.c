/*#include<stdio.h>
#include<conio.h>
#include<string.h>

void main()
{
    int i,count=0;
    char str[100],ch;
    printf("WRITE A SENTENCE:\n");
    gets(str);
    printf("ENTER THE CHARACTER YOU WANT THE FREQUENCY OF:\n");
    scanf(" %c",&ch);
    int length = strlen(str);

    for(i=0;i<length;i++)
    {
        if(ch==str[i])
            count++;
    }

    printf("\n THE CHARACTER %c HAS BEEN REPEATED %d TIMES IN ABOVE SENTENCE.",ch,count);

    getch();
}
*/
