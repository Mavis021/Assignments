/*#include<stdio.h>
#include<conio.h>
void main()
{
    int n,fact=1,i;
    printf("Enter an integer:");
    scanf("%d",&n);
    if(n>=0)
    {
     for (i=1;i<=n;i++)
     {
         fact=fact*i;
     }
    printf("Factorial of %d = %d",n,fact);
    }
    else
        printf("The factorial of %d cannot be computed.",n);
    getch();
}
*/
