
/*#include<stdio.h>
#include<conio.h>
int main()
{

   int rem,x,n,sum=0;
   printf("Enter a  positive number:");
   scanf("%d",&n);
   x=n;
   while(x!=0)
   {
       rem = x%10;
       sum = sum+rem;
       x = x/10;
   }
   printf("The sum of digits of %d = %d",n,sum);

    return 0;
}
*/
