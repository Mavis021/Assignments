/*#include<stdio.h>
#include<conio.h>

int fact(unsigned int a)
{
    if(a==0)
        return(1);
    else
        return(a*fact(a-1));
}

void main()
{
    unsigned int n;
    printf("ENTER THE NUMBER TO COMPUTE THE FACTORIAL:");
    scanf("%u",&n);

    printf("THE FACTORIAL OF %u = %u.",n,fact(n));

    getch();
}

*/
