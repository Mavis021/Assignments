/*#include<stdio.h>
#include<conio.h>

void main()
{
    char word[10];
    printf("ENTER A WORD:\n");
    gets(word);

     int length=strlen(word),c;

    for(int i=0;word[i]!='\0';i++)
    {if(word[length-i-1]==word[i])
        c++;}

        if(c==length)
            printf("THE GIVEN STRING IS A PALINDROME.");
    else
        printf("THE GIVEN STRING IS NOT A PALINDRME.");
    getch();
}
*/
