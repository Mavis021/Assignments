/*#include<stdio.h>
#include<conio.h>

void main()
{
    float marks[5];
    int i;
    printf("%d\n",marks);//address of first element of the array
    printf("Address of different array elements:\n");
    for(i=0;i<5;i++)
        printf("Address of element %d is %u\n",i,&marks[i]);//4 bytes of memory allocated to each enement of the array
    getch();
}
*/
