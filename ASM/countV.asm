TITLE to count the number of vowels
.MODEL SMALL
.STACK 32
.DATA
STRING LABEL BYTE
MAXLEN DB 20
ACCTUAL DB ?
INPUT DB 20 DUP('$')

.CODE
MAIN PROC FAR
MOV AX, @DATA
MOV DS, AX

MOV AH,00H           ;CLEAR SCREEN 
MOV AL,03H
INT 10H

MOV AH, 0AH
LEA DX,STRING
INT 21H

MOV SI,OFFSET STRING
MOV CH,0
MOV CL, ACCTUAL+2
MOV DH,0
HERE:MOV DL,[SI]
CMP DL,'a'
JE COUNT
CMP DL,'e'
JE COUNT
CMP DL,'i'
JE COUNT
CMP DL,'o'
JE COUNT
CMP DL,'u'
JE COUNT
SKIP:INC SI
LOOP HERE
JMP DISPLAY

COUNT:INC DH
JMP SKIP

DISPLAY:MOV AH,02H
MOV DL,0DH
INT 21H
MOV DL,0AH
INT 21H

MOV CX,10
MOV AL,DH
MOV AH,0
MOV BX,0

STEP3:MOV DX,0
DIV CX
ADD DX,30H
PUSH DX
INC BX
CMP AX,0
JA STEP3

MOV AH,02
MOV CX,BX
DISP:POP DX
INT 21H
LOOP DISP

MOV AH,4CH
INT 21H

MAIN ENDP
END MAIN
