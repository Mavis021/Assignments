/*#include<stdio.h>
#include<conio.h>
void main()
{
    char ch;
    printf("Enter any character:");
    scanf(" %c",&ch);
    if(ch>=65 && ch<=90)
        printf("\n The converted character is %c",ch+32);
    else if (ch>=97 && ch<=122)
        printf("\n The converted character is %c",ch-32);

    getch();
}
*/
