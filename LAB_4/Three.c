/*#include<stdio.h>
#include<conio.h>
#include<math.h>
void main()
{
    float a,b,c,s,A;
    printf("Enter the sides of the triangle a, b, c respectively:");
    scanf("%f%f%f",&a,&b,&c);
    if((a+b>c)&&(b+c>a)&&(a+c>b))
      {
    s=(a+b+c)/2;
    A= sqrt(s*(s-a)*(s-b)*(s-c));
    printf("The area of the given triangle is %f",A);
      }
else
    printf("Triangle cannot be formed by the given sides.");


    getch();
}
*/
